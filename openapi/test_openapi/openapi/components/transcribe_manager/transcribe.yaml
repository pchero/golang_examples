type: object
properties:
  id:
    type: string
    description: Transcribe id
  customer_id:
    type: string
    description: Customer ID
  reference_type:
    $ref: '#/components/schemas/Transcribereference_type'
  reference_id:
    type: string
    description: Call/Conference/Recording's ID
  status:
    $ref: '#/components/schemas/TranscribeStatus'
  language:
    type: string
    description: BCP47 type's language code.
  direction:
    $ref: '#/components/schemas/TranscribeDirection'
  tm_create:
    type: string
    format: date-time
    description: Timestamp when created
  tm_update:
    type: string
    format: date-time
    description: Timestamp when updated
  tm_delete:
    type: string
    format: date-time
    description: Timestamp when deleted
