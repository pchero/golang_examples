components:
  schemas:
    TranscribeDirection:
      $ref: 'transcribe_direction.yaml'
    Transcribereference_type:
      $ref: 'transcribe_reference_type.yaml'
    TranscribeStatus:
      $ref: 'transcribe_status.yaml'
    Transcribe:
      $ref: 'transcribe.yaml'

    TranscriptDirection:
      $ref: 'transcript_direction.yaml'
    Transcript:
      $ref: 'transcript.yaml'
