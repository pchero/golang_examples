components:
  schemas:
    TranscribeDirection:
      $ref: 'transcribe_direction.yaml'
    TranscribeReferenceType:
      $ref: 'transcribe_reference_type.yaml'
    TranscribeStatus:
      $ref: 'transcribe_status.yaml'
    Transcribe:
      $ref: 'transcribe.yaml'

    TranscriptDirection:
      $ref: 'transcript_direction.yaml'
    Transcript:
      $ref: 'transcript.yaml'
